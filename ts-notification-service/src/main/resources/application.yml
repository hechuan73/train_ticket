# HTTP Server
server:
  port: 17853  # HTTP (Tomcat) port

spring:
  cloud:
    nacos:
      discovery:
        server-addr: nacos-0.nacos-headless.default.svc.cluster.local,nacos-1.nacos-headless.default.svc.cluster.local,nacos-2.nacos-headless.default.svc.cluster.local
  application:
    name: ts-notification-service
  mail:
    host: smtp.163.com
    port: 465
    username: ${email_address:trainticket_notify@163.com}
    password: ${email_password:GZKSCXHBOLKMCLDQ}
    properties:
      mail:
        smtp:
          ssl:
            enable: true
          auth: true
          starttls:
            enable: true
            required: true
  datasource:
    url: jdbc:mysql://ts-notification-mysql:3306/ts-notification-mysql?useSSL=false
    username: root
    password: root
    driver-class-name: com.mysql.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update

  rabbitmq:
    host: ${rabbitmq_host:localhost}
    port: ${rabbitmq_port:5672}


swagger:
  controllerPackage: notification.controller
